# Appium Server 3.X 服务配置

# 服务配置
server:
  host: ${APPIUM_HOST}
  port: ${APPIUM_PORT}
  log_level: info
  log_file: ./logs/appium_server.log
  timeout: 120

# 服务参数
args:
  --use-plugins: "element-wait"
  --allow-insecure: "adb_shell"
  --deny-insecure: "none"